import { Image, StyleSheet, Platform, TouchableOpacity, View, Text } from 'react-native';
import React, { useContext } from 'react';
export default function ImageScreen() {
    return(
        <View>
            <Text> Import your image</Text>
        </View>
    )
}